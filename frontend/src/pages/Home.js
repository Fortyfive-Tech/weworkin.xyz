import React, {Fragment} from 'react'
import FilterableGrid from '../components/FilterableGrid'

const Home = () => {
    return <Fragment>
        <FilterableGrid/>
    </Fragment>
}

export default Home